
-- 1. Crear tabla de Usuarios
CREATE TABLE USUARIOS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE VARCHAR2(100) NOT NULL,
    CORREO VARCHAR2(100) UNIQUE NOT NULL,
    PASSWORD VARCHAR2(255) NOT NULL,
    ROL VARCHAR2(20) DEFAULT 'usuario'
);

-- 2. Crear tabla de Talleres
CREATE TABLE TALLERES (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE VARCHAR2(100) NOT NULL,
    DESCRIPCION VARCHAR2(255),
    CUPO NUMBER NOT NULL
);

-- 3. Crear tabla de Solicitudes
CREATE TABLE SOLICITUDES (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USUARIO_ID NUMBER REFERENCES USUARIOS(ID),
    TALLER_ID NUMBER REFERENCES TALLERES(ID),
    ESTADO VARCHAR2(20) DEFAULT 'pendiente'
);

-- 4. Insertar datos iniciales para probar
-- El password de admin es: admin123
INSERT INTO USUARIOS (NOMBRE, CORREO, PASSWORD, ROL) 
VALUES ('Administrador', 'admin@rolvin.com', '$2y$10$8K.uFhY7B9/fVv.yP8/HaeG.5gB0X0.fD6v1x5x6x7x8x9x0x', 'admin');

INSERT INTO TALLERES (NOMBRE, DESCRIPCION, CUPO) VALUES ('Tour Isla Tortuga', 'Snorkeling y playa', 10);
INSERT INTO TALLERES (NOMBRE, DESCRIPCION, CUPO) VALUES ('Bioluminiscencia', 'Tour nocturno en bote', 5);

COMMIT;